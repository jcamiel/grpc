syntax = "proto3";

package helloworld;

// Trivial baseline service — used as the simplest possible target
// for clients during the survey phase of the POC.
service Greeter {
  rpc SayHello (HelloRequest) returns (HelloReply);
}

message HelloRequest {
  string name = 1;
}

message HelloReply {
  string message = 1;
}
